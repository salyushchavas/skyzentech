import ErmPlaceholderPage from '@/components/erm/ErmPlaceholderPage';

export default function ApplicationInboxPage() {
  return (
    <ErmPlaceholderPage
      title="Application Inbox"
      subtitle="Pipeline of applications awaiting ERM action."
      phase={2}
    />
  );
}
