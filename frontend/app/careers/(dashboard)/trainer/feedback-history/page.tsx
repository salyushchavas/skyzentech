import StubPage from '@/components/trainer/StubPage';

export default function TrainerFeedbackHistoryPage() {
  return (
    <StubPage
      title="Feedback History"
      description="Audit-style timeline of every feedback decision this trainer has published, sourced from project_assignment_event_logs. Filterable by intern, project, decision."
      phase={4}
    />
  );
}
