import StubPage from '@/components/trainer/StubPage';

export default function TrainerFilesTemplatesPage() {
  return (
    <StubPage
      title="Files / Templates"
      description="ProjectTemplate library — reusable assignment blueprints (title, technology area, instructions Markdown, GitHub instructions, attached starter files). Shared across all trainers per doc §3. Instantiate a template into a fresh assignment from this page."
      phase={2}
    />
  );
}
