import StubPage from '@/components/trainer/StubPage';

export default function TrainerReportsPage() {
  return (
    <StubPage
      title="Reports"
      description="Doc-spec'd single export — monthly progress report + training summary + project completion history scoped to this trainer's interns. CSV / PDF download."
      phase={4}
    />
  );
}
