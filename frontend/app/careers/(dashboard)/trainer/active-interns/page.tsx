import StubPage from '@/components/trainer/StubPage';

export default function TrainerActiveInternsPage() {
  return (
    <StubPage
      title="Active Interns"
      description="Doc §5 list with 9 columns: Employee ID, full name, phone/email, technology title, start date, current month projects, weekly meeting status, evaluator status, timesheet status. Filtered by intern_lifecycles.trainer_id."
      phase={1}
    />
  );
}
