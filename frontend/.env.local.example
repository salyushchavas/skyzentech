# Also used by the /mail module's axios client (lib/mail-api.ts) — no separate
# mail base-URL var is needed; the mail UI is same-origin with careers.
NEXT_PUBLIC_API_URL=http://localhost:8080
RESEND_API_KEY=
